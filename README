🤖 บอทสร้างนิทานอัตโนมัติ (YouTube Auto-Uploader)

สคริปต์ Python ที่ออกแบบมาเพื่อสร้างสรรค์เนื้อหานิทานสำหรับเด็กก่อนนอน 🌙✨ และอัปโหลดไปยัง YouTube โดยอัตโนมัติเต็มรูปแบบ เปลี่ยนไอเดียของคุณให้เป็นวิดีโอที่พร้อมเผยแพร่ทุกวัน!

🚀 การติดตั้ง (Getting Started)

ทำตามขั้นตอนเหล่านี้เพื่อเตรียมโปรเจกต์ให้พร้อมใช้งาน

1. โคลนโปรเจกต์ (Clone Project)

ดาวน์โหลดหรือโคลนโปรเจกต์นี้ไปยังเครื่องของคุณ

git clone [https://github.com/your-username/your-repository-name.git](https://github.com/your-username/your-repository-name.git)
cd your-repository-name


2. สร้างสภาพแวดล้อม (Virtual Environment)

แนะนำให้ใช้ Virtual Environment (venv) เพื่อป้องกันไม่ให้ไลบรารีมีปัญหากับโปรเจกต์อื่น

# สำหรับ Windows
python -m venv venv
.\venv\Scripts\activate


3. ติดตั้งไลบรารีที่จำเป็น (Install Dependencies)

ติดตั้งไลบรารีทั้งหมดที่โปรเจกต์นี้ต้องการโดยใช้ไฟล์ requirement.txt

pip install -r requirement.txt


เนื้อหาไฟล์ requirement.txt

(คุณสามารถสร้างไฟล์นี้และคัดลอกเนื้อหานี้ไปวางได้)

# Google APIs (YouTube & Gemini)
google-api-python-client
google-auth-oauthlib
google-generativeai

# Video & Audio Processing
moviepy
soundfile
numpy

# Image Processing
Pillow

# HTTP Requests
requests

# Scheduling
schedule


🛠️ การตั้งค่า (Configuration)

ก่อนรันสคริปต์ คุณต้องตั้งค่าไฟล์และ API Keys ที่จำเป็นก่อน

1. API Keys

Gemini API Key:

คุณต้องมี GEMINI_API_KEY

ในโค้ด main.py ค้นหาบรรทัด GEMINI_API_KEY = "YOUR_API_KEY" และแทนที่ "YOUR_API_KEY" ด้วยคีย์ของคุณ

⚠️ เพื่อความปลอดภัย: ควรกำหนดค่านี้เป็น Environment Variable แทนการ Hardcode

YouTube Data API (OAuth 2.0):

ไปที่ Google Cloud Console เพื่อเปิดใช้งาน YouTube Data API v3 และสร้าง "ข้อมูลรับรอง" (Credentials) ประเภท "OAuth 2.0 Client IDs"

ดาวน์โหลดไฟล์ข้อมูลรับรองนั้นมา ซึ่งจะได้ไฟล์ชื่อ client_secrets.json

นำไฟล์ client_secrets.json มาวางไว้ในโฟลเดอร์หลักของโปรเจกต์ (ที่เดียวกับ main.py)

2. โฟลเดอร์และฟอนต์ (Folders & Fonts)

ในโฟลเดอร์หลักของโปรเจกต์ โปรดสร้างโฟลเดอร์และเตรียมไฟล์ดังต่อไปนี้:

source/ (โฟลเดอร์ว่างสำหรับเก็บไฟล์เสียงและภาพปกชั่วคราว)

output/ (โฟลเดอร์ว่างสำหรับเก็บไฟล์วิดีโอที่เสร็จแล้ว)

font/ (โฟลเดอร์สำหรับเก็บไฟล์ฟอนต์)

font/Sarabun-Bold.ttf (คุณต้องหาดาวน์โหลดไฟล์ฟอนต์นี้ หรือใช้ฟอนต์ .ttf อื่นที่คุณต้องการ แล้วไปแก้ไขชื่อในโค้ด main.py บรรทัด Fnt = ImageFont.truetype(...))

▶️ การใช้งาน (Usage)

เมื่อติดตั้งและตั้งค่าเสร็จแล้ว คุณสามารถรันสคริปต์ได้ 2 วิธี:

วิธีที่ 1: รันผ่าน Command Prompt (CMD)

เปิด Command Prompt หรือ Terminal

ไปที่โฟลเดอร์โปรเจกต์ของคุณ (เช่น cd C:\Users\YourName\MyYoutubeBot)

หากคุณใช้ venv ให้เปิดใช้งานก่อน: .\venv\Scripts\activate

สั่งรันสคริปต์:

python main.py


สำหรับการรันครั้งแรก (YouTube API):

สคริปต์จะเปิดหน้าต่างเบราว์เซอร์ขึ้นมา

ให้คุณล็อกอินบัญชี Google (บัญชีที่มีช่อง YouTube ที่คุณต้องการอัปโหลด)

กดยินยอม (Allow) เพื่ออนุญาตให้สคริปต์จัดการวิดีโอในช่องของคุณ

สคริปต์จะสร้างไฟล์ token.pickle ขึ้นมาเก็บไว้ เพื่อใช้ล็อกอินอัตโนมัติในครั้งถัดไป

วิธีที่ 2: รันผ่านไฟล์ .bat (สำหรับ Windows)

ไฟล์ .bat ช่วยให้คุณรันโปรแกรมได้ง่ายๆ เพียงแค่ดับเบิลคลิก

สร้างไฟล์:

คลิกขวาในโฟลเดอร์โปรเจกต์ (ที่เดียวกับ main.py)

เลือก New > Text Document

ตั้งชื่อไฟล์เป็น run.bat (ต้องแน่ใจว่านามสกุลเป็น .bat ไม่ใช่ .bat.txt)

แก้ไขไฟล์:

คลิกขวา ที่ไฟล์ run.bat > เลือก Edit (จะเปิดไฟล์ด้วย Notepad)

คัดลอกโค้ดด้านล่างนี้ไปวาง

⚠️ แก้ไข Path 2 จุดสำคัญ:

บรรทัดที่ 1 (cd /d ...): แก้ไขให้เป็นที่อยู่ของโฟลเดอร์โปรเจกต์ ของคุณ

บรรทัดที่ 2 ("C:\Python..."): แก้ไข Path แรกให้เป็นที่อยู่ของ python.exe ของคุณ และ Path ที่สองให้เป็นที่อยู่ main.py ของคุณ

(💡 วิธหา Path ของ python.exe: เปิด Command Prompt (cmd) พิมพ์ where python แล้วกด Enter)

ตัวอย่างโค้ดสำหรับไฟล์ run.bat (ที่ต้องแก้ไข)

@echo off
echo Starting Podcast Bot...

:: 1. เปลี่ยนไปที่โฟลเดอร์โปรเจกต์ (แก้ Path นี้)
cd /d "D:\MyBot"

echo Now in directory: %cd%
echo ====================================
echo Running Python script...

:: 2. รันสคริปต์ (แก้ Path python.exe และ main.py ให้ตรง)
"C:\Python311\python.exe" "D:\MyBot\main.py"

echo ====================================
echo Script finished.

:: 3. หยุดหน้าจอไว้ดูผลลัพธ์ หรือ Error
pause


บันทึก (Save) ไฟล์ run.bat

ดับเบิลคลิก ที่ไฟล์ run.bat เพื่อเริ่มทำงาน

⚙️ ฟังก์ชันเพิ่มเติม (Features)

การอัปโหลดอัตโนมัติ:

ในโค้ด main.py ภายในฟังก์ชัน mainprogram() บรรทัด uploadtoyoutube(...) ถูกปิดการใช้งาน (comment out) อยู่

หากต้องการให้อัปโหลดอัตโนมัติ ให้ลบเครื่องหมาย # ข้างหน้าบรรทัดนั้นออก

การตั้งเวลา (Schedule):

สคริปต์ถูกตั้งค่าให้ทำงานทุกวัน เวลา 12:00 น.

คุณสามารถแก้ไขเวลาได้ที่ส่วนท้ายของไฟล์ main.py (บรรทัด schedule.every().day.at("12:00").do(mainprogram))

❤️ สนับสนุน (Support)

ขอบคุณที่สนใจโปรเจกต์นี้ครับ! หากคุณชื่นชอบและอยากเลี้ยงค่าน้ำแดงมะนาวโซดา 🥤 ระหว่างเขียนโค้ด สามารถโดเนทได้ที่:

พร้อมเพย์ (PromptPay): 0932620499

ชื่อบัญชี: ปรเมศวร์ อุบลหอม