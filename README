🤖 บอทสร้างนิทานอัตโนมัติ (YouTube Auto-Uploader)

โปรเจกต์นี้เป็นสคริปต์ Python ที่ทำงานอัตโนมัติเต็มรูปแบบในการสร้างวิดีโอนิทานสำหรับเด็กและอัปโหลดไปยัง YouTube โดยกระบวนการทั้งหมดทำงานเชื่อมต่อกันดังนี้:

สร้างเนื้อเรื่อง: ใช้ Gemini API (โมเดล gemini-1.5-flash) ในการเขียนสคริปต์นิทานก่อนนอน (ความยาวประมาณ 15-20 นาที)

สร้างเสียงอ่าน: ใช้ Gemini TTS API (โมเดล gemini-1.5-flash-preview-tts) เพื่อแปลงสคริปต์นิทานทั้งเรื่องให้เป็นไฟล์เสียง (.wav)

สร้างภาพปก: ใช้ Gemini สร้าง Prompt ที่สร้างสรรค์สำหรับภาพปก จากนั้นใช้ Prompt นั้นเรียก API ของ pollinations.ai เพื่อสร้างและดาวน์โหลดภาพพื้นหลัง

แก้ไขภาพปก: ใช้ไลบรารี Pillow (PIL) เพื่อนำชื่อเรื่องที่ได้จาก Gemini มาวาดลงบนภาพพื้นหลังที่ดาวน์โหลดมา

สร้างวิดีโอ: ใช้ MoviePy เพื่อรวมองค์ประกอบต่างๆ เข้าด้วยกัน:

นำภาพปกที่แก้ไขแล้วมาเป็นพื้นหลัง

นำไฟล์เสียงมาใส่

สร้าง Audio Visualizer (แท่งกราฟเสียงที่เต้นตามความดัง) แบบเรียลไทม์

กำหนดความยาววิดีโอทั้งหมดให้เท่ากับความยาวของไฟล์เสียง

อัปโหลด: (ส่วนนี้มีฟังก์ชันเตรียมไว้ แต่ในโค้ดหลักถูก comment ปิดไว้) ใช้ YouTube Data API v3 เพื่ออัปโหลดวิดีโอที่เสร็จแล้วไปยังช่อง YouTube พร้อมตั้งค่า Title, Description, Tags และ Thumbnail

ทำงานอัตโนมัติ: ใช้ schedule เพื่อสั่งให้สคริปต์นี้ทำงานซ้ำอัตโนมัติ (ในโค้ดตั้งไว้ที่ 12:00 น. ทุกวัน)

⚙️ การทำงานของฟังก์ชันหลัก

1. การยืนยันตัวตน (Authentication)

get_authenticated_service(): จัดการการเชื่อมต่อกับ YouTube API โดยใช้ OAuth 2.0

ครั้งแรกที่รัน สคริปต์จะเปิดเบราว์เซอร์ให้คุณล็อกอินและอนุญาต

หลังจากนั้น จะบันทึกสิทธิ์ลงในไฟล์ token.pickle เพื่อใช้ในครั้งต่อไป

2. ส่วนของ Gemini (AI Content)

generate_story_from_gemini(): สั่งให้ Gemini เขียนนิทานตาม Prompt ที่กำหนดไว้ (System Prompt) และคืนค่าเป็น Text

generate_image_from_gemini(title): ใช้ Gemini ช่วยคิด Prompt สำหรับสร้างภาพ แล้วไปเรียก pollinations.ai เพื่อดาวน์โหลดภาพพื้นหลัง (มี time.sleep(30) เพื่อรอให้ AI สร้างภาพ)

speak(txt): ส่ง Text ทั้งหมดไปให้ Gemini TTS สร้างเป็นไฟล์เสียงและบันทึกเป็น source/out.wav

3. ส่วนของวิดีโอ (MoviePy & PIL)

Editimage(title): ใช้ Pillow เปิดภาพพื้นหลัง (file_nameBG) ขึ้นมา แล้ววาดข้อความ title ลงไปตรงกลางค่อนล่าง บันทึกเป็นภาพใหม่ (file_nameBG_Edit)

CreateVideo():

โหลดไฟล์เสียง source/out.wav เพื่อเอา duration (ความยาววินาที)

โหลดภาพ file_nameBG_Edit และตั้งค่า duration ให้เท่ากับเสียง

จุดเด่น: สร้าง visualizer_clip โดยใช้ VideoClip ซึ่งจะเรียกฟังก์ชัน make_visualizer_frame ในทุกๆ เฟรม

make_visualizer_frame(t): คำนวณค่าความดัง (Amplitude) ของเสียง ณ เวลา (t) นั้นๆ และวาดแท่งสี่เหลี่ยมสีขาวที่มีความสูงตามความดัง ทำให้เกิดเป็น Visualizer ที่ขยับตามเสียงพูด

สุดท้ายใช้ CompositeVideoClip รวมภาพพื้นหลังและ Visualizer เข้าด้วยกัน แล้วใช้ .with_audio() เพื่อใส่เสียง ก่อนจะ .write_videofile() ออกมาเป็นไฟล์ .mp4

4. การอัปโหลด (YouTube API)

upload_video(): จัดการกระบวนการอัปโหลดไฟล์วิดีโอแบบ resumable (เหมาะสำหรับไฟล์ใหญ่) และแสดงความคืบหน้า

upload_thumbnail(): (ฟังก์ชันนี้มีอยู่ แต่ไม่ได้ถูกเรียกใช้ใน uploadtoyoutube) ใช้สำหรับอัปโหลดภาพหน้าปกแยกต่างหาก

5. การทำงานหลัก (Main Workflow)

mainprogram(): เป็นฟังก์ชันที่ควบคุมลำดับการทำงานทั้งหมด ตั้งแต่สร้างเรื่อง -> สร้างรูป -> แก้รูป -> สร้างเสียง -> สร้างวิดีโอ

if __name__ == "__main__"::

สั่ง mainprogram() ทำงานทันที 1 ครั้ง

ตั้งเวลา schedule ให้ mainprogram() ทำงานอีกครั้งตอน 12:00 น. ของทุกวัน

เข้าสู่ while True loop เพื่อให้ schedule คอยตรวจสอบเวลาและทำงานตามที่ตั้งไว้

⚠️ ข้อควรระวังและการตั้งค่า

API Keys:

คุณต้องมี GEMINI_API_KEY และใส่ไว้ในสคริปต์ (ในโค้ดมีการ Hardcode ไว้ ซึ่งไม่ปลอดภัย ควรเปลี่ยนไปใช้ Environment Variable)

คุณต้องมีไฟล์ client_secrets.json ที่ดาวน์โหลดจาก Google Cloud Console สำหรับ YouTube API

ไฟล์และโฟลเดอร์:

คุณต้องสร้างโฟลเดอร์ source, output, และ font

คุณต้องมีไฟล์ฟอนต์ font/Sarabun-Bold.ttf (หรือเปลี่ยน path ในโค้ด)

การอัปโหลด:

ในฟังก์ชัน mainprogram() บรรทัด uploadtoyoutube(...) ถูกปิดการใช้งาน (comment out) อยู่ หากต้องการให้อัปโหลดอัตโนมัติ ต้องลบ # ข้างหน้าออก

การอัปโหลด Thumbnail ต้องมีการยืนยันตัวตนช่อง YouTube ด้วยเบอร์โทรศัพท์ก่อน

🚀 การรันสคริปต์ (ด้วยไฟล์ .bat)

ไฟล์ contentsweetdream.bat (หรือไฟล์ .bat อื่นๆ) เป็นสคริปต์สำหรับ Windows ที่ช่วยให้คุณรันโปรแกรม Python นี้ได้ง่ายๆ เพียงแค่ดับเบิลคลิก แทนที่จะต้องเปิด Command Prompt (cmd) และพิมพ์คำสั่งเองทุกครั้ง

A. อธิบายการทำงาน (ทีละบรรทัด)

นี่คือคำอธิบายว่าโค้ดในไฟล์ contentsweetdream.bat ทำอะไรบ้าง:

:: ปิดไม่ให้แสดงตัวคำสั่งขณะที่มันทำงาน (ทำให้หน้าจอดูสะอาด)
@echo off

:: แสดงข้อความว่า "Starting Podcast Bot..."
echo Starting Podcast Bot...

:: 1. (สำคัญมาก) เปลี่ยนไดเรกทอรี
:: cd /d คือคำสั่งเปลี่ยนไดเรกทอรี และ /d คืออนุญาตให้เปลี่ยน Drive ด้วย (เช่น จาก C: ไป E:)
:: นี่จำเป็นมากเพื่อให้ Python หาไฟล์ต่างๆ ในโฟลเดอร์ source, font, output เจอ
cd /d "e:/BOT/BotCreatepodcast/"

:: แสดงที่อยู่ปัจจุบัน (เพื่อตรวจสอบว่ามาถูกโฟลเดอร์แล้ว)
echo Now in directory: %cd%
echo ====================================
echo Running Python script...

:: 2. (สำคัญมาก) สั่งรันสคริปต์ Python
:: ส่วนแรก คือ Path เต็มของ "python.exe" (ตัวแปรภาษา)
:: ส่วนที่สอง คือ Path เต็มของ "ไฟล์ .py" ที่เราต้องการรัน
C:/Users/Neocluster/AppData/Local/Programs/Python/Python310/python.exe e:/BOT/BotCreatepodcast/main.py

echo ====================================
echo Script finished.

:: 3. หยุดหน้าจอไว้ชั่วคราว
:: เพื่อให้เราอ่านข้อความ Error หรือผลลัพธ์ได้ทัน (ถ้าไม่ใส่ หน้าจอ cmd จะปิดไปทันที)
pause


B. ⚠️ วิธีตั้งค่า (สำหรับเครื่องของคุณ)

เพื่อให้ไฟล์ .bat นี้ทำงานบนเครื่องคอมพิวเตอร์ ของคุณ ได้ คุณต้องแก้ไข Path (ที่อยู่ไฟล์) 2-3 จุดสำคัญ ดังนี้:

สร้างไฟล์:

คลิกขวาในโฟลเดอร์โปรเจกต์ของคุณ (ที่เดียวกับ main.py)

เลือก New > Text Document

ตั้งชื่อไฟล์เป็น run.bat หรือ contentsweetdream.bat (ต้องแน่ใจว่านามสกุลเป็น .bat ไม่ใช่ .bat.txt)

คลิกขวา ที่ไฟล์ .bat > เลือก Edit (จะเปิดไฟล์ด้วย Notepad)

แก้ไข Path 2 บรรทัดสำคัญ:

บรรทัดที่ 1 (ที่อยู่โปรเจกต์):

แก้จาก: cd /d "e:/BOT/BotCreatepodcast/"

แก้เป็น: ที่อยู่ของโฟลเดอร์โปรเจกต์ ของคุณ

(วิธีหา: เปิดโฟลเดอร์โปรเจกต์ใน File Explorer แล้วคลิกขวาที่ Address Bar > Copy address)

ตัวอย่าง: cd /d "C:\Users\YourName\Documents\MyYoutubeBot\"

บรรทัดที่ 2 (ที่อยู่ Python และไฟล์สคริปต์):

แก้จาก: C:/Users/Neocluster/AppData/Local/Programs/Python/Python310/python.exe e:/BOT/BotCreatepodcast/main.py

แก้เป็น: "[Path ของ python.exe ของคุณ]" "[Path ของ main.py ของคุณ]"

💡 วิธหา Path ของ python.exe:

เปิด Command Prompt (cmd)

พิมพ์ where python แล้วกด Enter

มันจะแสดง Path ของ python.exe ที่เครื่องคุณเรียกใช้ (เช่น C:\Users\YourName\AppData\Local\Programs\Python\Python312\python.exe)

ให้คัดลอก Path นั้นไปวางแทนส่วนแรก

C. ตัวอย่างไฟล์ .bat ที่แก้ไขแล้ว

สมมติว่าโปรเจกต์ของคุณอยู่ที่ D:\MyBot และ python.exe ของคุณอยู่ที่ C:\Python311\python.exe ไฟล์ .bat ที่ถูกต้องควรจะมีหน้าตาแบบนี้:

@echo off
echo Starting Podcast Bot...

:: 1. เปลี่ยนไปที่โฟลเดอร์โปรเจกต์
cd /d "D:\MyBot"

echo Now in directory: %cd%
echo ====================================
echo Running Python script...

:: 2. รันสคริปต์ (แก้ Path python.exe และ main.py ให้ตรง)
"C:\Python311\python.exe" "D:\MyBot\main.py"

echo ====================================
echo Script finished.
pause


❤️ สนับสนุน (Support)

ขอบคุณที่สนใจโปรเจกต์นี้ครับ! หากคุณชื่นชอบและอยากเลี้ยงค่าน้ำแดงมะนาวโซดา 🥤 ระหว่างเขียนโค้ด สามารถโดเนทได้ที่:

พร้อมเพย์ (PromptPay): 0932620499

ชื่อบัญชี: ปรเมศวร์ อุบลหอม

📋 ไฟล์ requirement.txt

นี่คือรายการไลบรารี Python ที่จำเป็นสำหรับรันสคริปต์นี้ คุณสามารถบันทึกเป็นไฟล์ requirement.txt และติดตั้งทั้งหมดได้ด้วยคำสั่ง pip install -r requirement.txt

# Google APIs (YouTube & Gemini)
google-api-python-client
google-auth-oauthlib
google-generativeai

# Video & Audio Processing
moviepy
soundfile
numpy

# Image Processing
Pillow

# HTTP Requests
requests

# Scheduling
schedule
