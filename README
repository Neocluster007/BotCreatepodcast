แน่นอนครับ นี่คือเนื้อหาทั้งหมดสำหรับไฟล์ `README.md` ของคุณ คุณสามารถคัดลอกข้อความทั้งหมดด้านล่างนี้ไปวางในไฟล์ `README.md` ในโปรเจกต์ของคุณได้เลยครับ

````markdown
# 🤖 บอทสร้างนิทานอัตโนมัติ (YouTube Auto-Uploader)

สคริปต์ Python ที่ออกแบบมาเพื่อสร้างสรรค์เนื้อหานิทานสำหรับเด็กก่อนนอน 🌙✨ และอัปโหลดไปยัง YouTube โดยอัตโนมัติเต็มรูปแบบ เปลี่ยนไอเดียของคุณให้เป็นวิดีโอที่พร้อมเผยแพร่ทุกวัน!



---

## ⚙️ กระบวนการทำงาน (Workflow)

โปรเจกต์นี้ทำงานเชื่อมต่อกันอัตโนมัติ ตั้งแต่การสร้างเนื้อหาไปจนถึงการสร้างไฟล์วิดีโอ:

1.  📝 **สร้างเนื้อเรื่อง:**
    ใช้ Gemini API (gemini-1.5-flash) ในการเขียนสคริปต์นิทานก่อนนอน (ความยาวประมาณ 15-20 นาที)

2.  🎙️ **สร้างเสียงอ่าน:**
    ใช้ Gemini TTS API เพื่อแปลงสคริปต์นิทานทั้งเรื่องให้เป็นไฟล์เสียงคุณภาพสูง (`.wav`)

3.  🎨 **สร้างภาพปก:**
    ใช้ Gemini ช่วยสร้าง Prompt ที่สร้างสรรค์สำหรับภาพปก จากนั้นเรียก API ของ **pollinations.ai** เพื่อสร้างและดาวน์โหลดภาพพื้นหลัง

4.  🖌️ **แก้ไขภาพปก:**
    ใช้ไลบรารี **Pillow (PIL)** เพื่อนำ "ชื่อเรื่อง" ที่ได้จาก Gemini มาวาดลงบนภาพพื้นหลังที่ดาวน์โหลดมา

5.  🎬 **สร้างวิดีโอ:**
    ใช้ **MoviePy** เพื่อรวมองค์ประกอบทั้งหมดเข้าด้วยกัน:
    * ภาพปกที่แก้ไขแล้ว (เป็นพื้นหลัง)
    * ไฟล์เสียงนิทาน
    * **Audio Visualizer** (แท่งกราฟเสียงที่ขยับตามเสียงพูดแบบเรียลไทม์)
    * กำหนดความยาววิดีโอให้เท่ากับความยาวของไฟล์เสียงพอดี

6.  🚀 **อัปโหลด (ทางเลือก):**
    มีฟังก์ชันที่เตรียมไว้ (ถูก comment out) สำหรับใช้ **YouTube Data API v3** เพื่ออัปโหลดวิดีโอ, ตั้งค่า Title, Description, Tags และ Thumbnail

7.  ⏰ **ทำงานอัตโนมัติ:**
    ใช้ไลบรารี **schedule** เพื่อสั่งให้สคริปต์นี้ทำงานซ้ำอัตโนมัติ (ตั้งค่าเริ่มต้นไว้ที่ 12:00 น. ทุกวัน)

---

## 💻 เทคโนโลยีที่ใช้

* **Python 3.10+**
* **Google APIs:**
    * `google-generativeai` (Gemini 1.5 Flash & TTS)
    * `google-api-python-client` (YouTube Data API v3)
    * `google-auth-oauthlib`
* **Video & Audio:**
    * `moviepy`
    * `soundfile`
    * `numpy`
* **Image Processing:**
    * `Pillow`
* **Others:**
    * `requests` (สำหรับดาวน์โหลดภาพจาก pollinations.ai)
    * `schedule` (สำหรับตั้งเวลาทำงาน)

---

## 🚀 การติดตั้ง (Getting Started)

ทำตามขั้นตอนเหล่านี้เพื่อเตรียมโปรเจกต์ให้พร้อมใช้งาน

### 1. โคลนโปรเจกต์ (Clone)

```bash
git clone [https://github.com/your-username/your-repository-name.git](https://github.com/your-username/your-repository-name.git)
cd your-repository-name
````

### 2\. สร้างสภาพแวดล้อม (Environment)

แนะนำให้ใช้ Virtual Environment เพื่อจัดการ Dependencies:

```bash
# สำหรับ Windows
python -m venv venv
.\venv\Scripts\activate
```

### 3\. ติดตั้งไลบรารี (Install Dependencies)

ติดตั้งไลบรารีที่จำเป็นทั้งหมดจากไฟล์ `requirement.txt`:

```bash
pip install -r requirement.txt
```

**เนื้อหาไฟล์ `requirement.txt`:**

```txt
# Google APIs (YouTube & Gemini)
google-api-python-client
google-auth-oauthlib
google-generativeai

# Video & Audio Processing
moviepy
soundfile
numpy

# Image Processing
Pillow

# HTTP Requests
requests

# Scheduling
schedule
```

-----

## 🛠️ การตั้งค่า (Configuration)

ก่อนรันสคริปต์ คุณต้องตั้งค่าไฟล์และ API Keys ที่จำเป็นก่อน

### 1\. API Keys

  * **Gemini API Key:**

      * คุณต้องมี `GEMINI_API_KEY`
      * ในโค้ดมีการ Hardcode ไว้ (`GEMINI_API_KEY = "YOUR_API_KEY"`)
      * ⚠️ **เพื่อความปลอดภัย:** ควรกำหนดค่านี้เป็น Environment Variable แทนการ Hardcode

  * **YouTube Data API (OAuth 2.0):**

      * คุณต้องไปที่ Google Cloud Console เพื่อเปิดใช้งาน YouTube Data API v3 และดาวน์โหลดไฟล์ `client_secrets.json`
      * นำไฟล์ `client_secrets.json` มาวางไว้ในโฟลอร์หลักของโปรเจกต์
      * ครั้งแรกที่รัน (หรือเมื่อ `token.pickle` หมดอายุ) สคริปต์จะเปิดเบราว์เซอร์ให้คุณล็อกอินและอนุญาตสิทธิ์

### 2\. โฟลเดอร์และฟอนต์ (Folders & Fonts)

โปรดสร้างโฟลเดอร์และเตรียมไฟล์ดังต่อไปนี้ในไดเรกทอรีหลักของโปรเจกต์:

  * `source/` (สำหรับเก็บไฟล์เสียงและภาพปก)
  * `output/` (สำหรับเก็บไฟล์วิดีโอที่เสร็จแล้ว)
  * `font/` (สำหรับเก็บไฟล์ฟอนต์)
  * `font/Sarabun-Bold.ttf` (ไฟล์ฟอนต์ที่ใช้ หากต้องการใช้ฟอนต์อื่น ต้องแก้ไข Path ในฟังก์ชัน `Editimage()`)

### 3\. การเปิดใช้งานการอัปโหลด

ในฟังก์ชัน `mainprogram()` บรรทัด `uploadtoyoutube(...)` ถูกปิดการใช้งาน (comment out) อยู่ หากต้องการให้อัปโหลดอัตโนมัติ ให้ลบเครื่องหมาย `#` ข้างหน้าออก

> **หมายเหตุ:** การอัปโหลด Thumbnail ผ่าน API อาจต้องการการยืนยันตัวตนช่อง YouTube ด้วยเบอร์โทรศัพท์ก่อน

-----

## ▶️ การใช้งาน (Usage)

คุณสามารถรันสคริปต์ได้โดยตรงผ่านคำสั่ง:

```bash
python main.py
```

อย่างไรก็ตาม เพื่อความสะดวกสำหรับผู้ใช้ Windows ได้มีการเตรียมไฟล์ `.bat` ไว้ให้

### การรันผ่านไฟล์ `.bat` (สำหรับ Windows)

ไฟล์ `.bat` (เช่น `contentsweetdream.bat`) เป็นสคริปต์สำหรับ Windows ที่ช่วยให้คุณรันโปรแกรม Python นี้ได้ง่ายๆ เพียงแค่ดับเบิลคลิก แทนที่จะต้องเปิด Command Prompt (cmd) และพิมพ์คำสั่งเองทุกครั้ง

#### A. อธิบายการทำงาน (ทีละบรรทัด)

นี่คือคำอธิบายว่าโค้ดในไฟล์ `.bat` ตัวอย่างทำอะไรบ้าง:

```batch
:: ปิดไม่ให้แสดงตัวคำสั่งขณะที่มันทำงาน (ทำให้หน้าจอดูสะอาด)
@echo off

:: แสดงข้อความว่า "Starting Podcast Bot..."
echo Starting Podcast Bot...

:: 1. (สำคัญมาก) เปลี่ยนไดเรกทอรีไปยังที่อยู่ของโปรเจกต์
:: cd /d คือคำสั่งเปลี่ยนไดเรกทอรี และ /d คืออนุญาตให้เปลี่ยน Drive ด้วย
cd /d "e:/BOT/BotCreatepodcast/"

:: แสดงที่อยู่ปัจจุบัน (เพื่อตรวจสอบว่ามาถูกโฟลเดอร์แล้ว)
echo Now in directory: %cd%
echo ====================================
echo Running Python script...

:: 2. (สำคัญมาก) สั่งรันสคริปต์ Python
:: ส่วนแรก คือ Path เต็มของ "python.exe" (ตัวแปรภาษา)
:: ส่วนที่สอง คือ Path เต็มของ "ไฟล์ .py" ที่เราต้องการรัน
C:/Users/Neocluster/AppData/Local/Programs/Python/Python310/python.exe e:/BOT/BotCreatepodcast/main.py

echo ====================================
echo Script finished.

:: 3. หยุดหน้าจอไว้ชั่วคราว
:: เพื่อให้เราอ่านข้อความ Error หรือผลลัพธ์ได้ทัน (ถ้าไม่ใส่ หน้าจอ cmd จะปิดไปทันที)
pause
```

#### B. ⚠️ วิธีตั้งค่า (สำหรับเครื่องของคุณ)

เพื่อให้ไฟล์ `.bat` นี้ทำงานบนเครื่องคอมพิวเตอร์ **ของคุณ** ได้ คุณต้องแก้ไข Path (ที่อยู่ไฟล์) 2-3 จุดสำคัญ ดังนี้:

1.  **สร้างไฟล์:**

      * คลิกขวาในโฟลเดอร์โปรเจกต์ของคุณ (ที่เดียวกับ `main.py`)
      * เลือก **New \> Text Document**
      * ตั้งชื่อไฟล์เป็น `run.bat` (ต้องแน่ใจว่านามสกุลเป็น `.bat` ไม่ใช่ `.bat.txt`)

2.  **แก้ไขไฟล์:**

      * คลิกขวา ที่ไฟล์ `run.bat` \> เลือก **Edit** (จะเปิดไฟล์ด้วย Notepad)

3.  **แก้ไข Path 2 บรรทัดสำคัญ:**

      * **บรรทัดที่ 1 (ที่อยู่โปรเจกต์):**

          * **แก้จาก:** `cd /d "e:/BOT/BotCreatepodcast/"`
          * **แก้เป็น:** ที่อยู่ของโฟลเดอร์โปรเจกต์ **ของคุณ**
          * *(วิธีหา: เปิดโฟลเดอร์โปรเจกต์ใน File Explorer แล้วคลิกขวาที่ Address Bar \> Copy address)*
          * **ตัวอย่าง:** `cd /d "C:\Users\YourName\Documents\MyYoutubeBot\"`

      * **บรรทัดที่ 2 (ที่อยู่ Python และไฟล์สคริปต์):**

          * **แก้จาก:** `C:/Users/Neocluster/AppData/Local/Programs/Python/Python310/python.exe e:/BOT/BotCreatepodcast/main.py`
          * **แก้เป็น:** `"[Path ของ python.exe ของคุณ]" "[Path ของ main.py ของคุณ]"`
          * *(💡 **วิธหา Path ของ python.exe:** เปิด Command Prompt (cmd) พิมพ์ `where python` แล้วกด Enter)*

#### C. ตัวอย่างไฟล์ .bat ที่แก้ไขแล้ว

สมมติว่าโปรเจกG:j `D:\MyBot` และ python.exe ของคุณอยู่ที่ `C:\Python311\python.exe` ไฟล์ `run.bat` ที่ถูกต้องควรจะมีหน้าตาแบบนี้:

```batch
@echo off
echo Starting Podcast Bot...

:: 1. เปลี่ยนไปที่โฟลเดอร์โปรเจกต์
cd /d "D:\MyBot"

echo Now in directory: %cd%
echo ====================================
echo Running Python script...

:: 2. รันสคริปต์ (แก้ Path python.exe และ main.py ให้ตรง)
"C:\Python311\python.exe" "D:\MyBot\main.py"

echo ====================================
echo Script finished.
pause
```

-----

## ❤️ สนับสนุน (Support)

ขอบคุณที่สนใจโปรเจกต์นี้ครับ\! หากคุณชื่นชอบและอยากเลี้ยงค่าน้ำแดงมะนาวโซดา 🥤 ระหว่างเขียนโค้ด สามารถโดเนทได้ที่:

  * **พร้อมเพย์ (PromptPay):** `0932620499`
  * **ชื่อบัญชี:** ปรเมศวร์ อุบลหอม

<!-- end list -->

```
```